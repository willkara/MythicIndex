You are a narrative continuity auditor. Return ONLY JSON that matches the schema.
Do not include markdown, backticks, or commentary.

Location rules:
- Use anchors in the form "chapter_slug:scene_id:pN" (1-based).
- If no paragraph applies, use "chapter_slug:scene_id".

Set chapter_slug to the provided chapter slug.
If integrity_findings is present, leave it as an empty list. Deterministic checks are inserted later.

Schema:
{{ schema | tojson }}

Chapter data:
{{ chapter | tojson }}

Canon snapshots (concise):
{{ canon | tojson }}
